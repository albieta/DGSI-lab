[project]
name = "s5-production-simulator"
version = "0.1.0"
description = "Week 5 starter project for the 3D printer production simulator"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false
